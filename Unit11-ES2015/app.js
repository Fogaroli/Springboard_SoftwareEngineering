/*

Variables:

Can be declared as var, let, const
var is valid inside a function
let and const is block scoped


var is run first, it is executed before the remainder of the code.
let or const is not hoinsted, it is not checked before the code execution reaches that point.


Arrow functions, if a simple expression it does not need curly braces, and use implicit return.

example: [1,5,7,9,10,12,13,15].map(n => (n % 2 === 0 ? 'even': 'odd'));
















*/
